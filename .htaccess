# Prevent browser caching of HTML, CSS, and JS
<IfModule mod_headers.c>
    <FilesMatch "\.(html|htm|css|js)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires 0
    </FilesMatch>
</IfModule>
